# DEPRECATED: Diese Datei wird nicht mehr als systemd EnvironmentFile verwendet.
# Grund: systemd expandiert $VAR in ExecStart ohne Word-Splitting - der gesamte
# JAVA_OPTS-String wird als ein einziges Argument uebergeben.
# Die JVM-Optionen stehen nun direkt als separate Argumente im sabiFE.service.
#
# Java17: JAVA_OPTS=-Djava.io.tmpdir=/var/sabi/tmp -XX:-UsePerfData -XX:+UseZGC -XX:+ZGenerational -Xmx256m -Xms96m
# Java21+: JAVA_OPTS=-Djava.io.tmpdir=/var/sabi/tmp -XX:-UsePerfData -Xmx256m -Xms96m -XX:MaxMetaspaceSize=160m
